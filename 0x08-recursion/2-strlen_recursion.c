#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - Returns Length of String
 * @s: string
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int _strlen_recursion(char *s)
{
	int n;
	char _strlen_recursion[] = "Corbin Coleman";

	n = strlen(_strlen_recursion);
}
